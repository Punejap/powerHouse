import java.io.IOException;
import java.lang.reflect.Array;
import java.net.MalformedURLException;
import java.net.URL;
import java.net.URLConnection;
import java.util.ArrayList;
import java.util.HashMap;

//a zone groups a set of components together - living room, dining room, etc...
//this class will include methods to quickly add various components to multiple (nested) array lists
    public class Zone {

/*
        public void powerOn() throws IOException {
            for (Component component : componentsList) {
                URL powerToggle = new URL("http://" + component.ip + "/cm?cmnd=Power%20On");
                URLConnection con = powerToggle.openConnection();
                con.getInputStream();
            }
        }

        public void powerOff() throws IOException {
            for (Component component : componentsList) {
                URL powerToggle = new URL("http://" + component.ip + "/cm?cmnd=Power%20off");
                URLConnection con = powerToggle.openConnection();
                con.getInputStream();
            }
        }
*/
        public Zone() {

        }

    }

